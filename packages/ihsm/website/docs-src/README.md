# Documentation sources (committed)

Hand-written pages live here; `npm run sync:docs` copies them into gitignored `website/docs/` and
runs generators for reference + testing chapters.

| Committed source | Generated in `website/docs/` |
|------------------|------------------------------|
| `intro.mdx` | Home (`/`) |
| `reference/REFERENCE.md` | Reference (`/reference`) |
| `reference/TESTING.md` | Deterministic testing (`/testing`) |

Also generated: `website/sidebars.ts`.
