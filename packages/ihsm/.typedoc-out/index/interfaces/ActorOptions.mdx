Optional tuning passed as the **last** argument to [makeActor](../functions/makeActor.mdx) / [testing!makeTestActor](../../testing/functions/makeTestActor.mdx),
after the three mandatory positional arguments (`topState`, `ctx`, `port`). Every field has a
sensible default; omit the whole object to take them all.

## Type Parameters

### Context

`Context`

Domain context type

### Public

`Public` *extends* `undefined` \| \{ \}

Public protocol

### Internal

`Internal` *extends* `object` = \{ \}

Internal protocol

## Properties

### initialize?

> `optional` **initialize?**: `boolean`

Run the initial `@InitialState` walk (default `true`).

***

### traceLevel?

> `optional` **traceLevel?**: [`TraceLevel`](../enumerations/TraceLevel.mdx)

Initial [TraceLevel](../enumerations/TraceLevel.mdx) (default [TraceLevel.DEBUG](../enumerations/TraceLevel.mdx#debug)).

***

### traceWriter?

> `optional` **traceWriter?**: [`TraceWriter`](TraceWriter.mdx)

[TraceWriter](TraceWriter.mdx) implementation (default: prefixes with state name, logs to `console`).

***

### dispatchErrorCallback?

> `optional` **dispatchErrorCallback?**: [`DispatchErrorCallback`](DispatchErrorCallback.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Public`, `Internal`\>\>

Last-resort error hook (default: trace + rethrow).
