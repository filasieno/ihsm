> `const` **dispatchContext**: `object`

Lazy Node AsyncLocalStorage for non-production deadlock detection.
State lives in a closure — no exported mutable slot.

## Type Declaration

### get

> **get**: () => \{ `run`: `R`; `getStore`: `unknown`; \} \| `undefined`

#### Returns

\{ `run`: `R`; `getStore`: `unknown`; \} \| `undefined`

### resetInit

> **resetInit**: () => `void`

#### Returns

`void`

### markUnavailable

> **markUnavailable**: () => `void`

#### Returns

`void`
