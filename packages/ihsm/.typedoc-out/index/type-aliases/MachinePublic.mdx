> **MachinePublic**\<`T`\> = `T` *extends* `object` ? `Public` : `never`

Extracts the **public** protocol from a [TopState](../classes/TopState.mdx) subclass.

## Type Parameters

### T

`T`

A [TopState](../classes/TopState.mdx) subclass (instance type)
