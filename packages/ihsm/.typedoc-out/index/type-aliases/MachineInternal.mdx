> **MachineInternal**\<`T`\> = `T` *extends* `object` ? `Internal` *extends* \{ \} \| `undefined` ? `Internal` : `never` : `never`

Extracts the **internal** protocol from a [TopState](../classes/TopState.mdx) subclass. The result is always
within `{} | undefined`, so it can drive [PostedEvent](PostedEvent.mdx) / [EventPayload](EventPayload.mdx) directly.

## Type Parameters

### T

`T`

A [TopState](../classes/TopState.mdx) subclass (instance type)
