> **ServiceName**\<`Protocol`, `EventName`\> = `Protocol` *extends* `undefined` ? `string` : `EventName` *extends* keyof [`State`](../interfaces/State.mdx)\<`any`, `any`\> ? `never` : `EventName` *extends* keyof `Protocol` ? [`IsServiceMethod`](IsServiceMethod.mdx)\<`Protocol`\[`EventName`\]\> *extends* `true` ? `EventName` : `never` : `never`

Valid first argument to [Hsm.call](../interfaces/Hsm.mdx#call) — protocol keys whose handlers use the service signature
`(resolve, reject, ...payload)`. Reserved [State](../interfaces/State.mdx) names and **event-shaped** keys become
`never`, so `call('open')` on a void event is a compile error (proposal T2).

## Type Parameters

### Protocol

`Protocol`

Machine vocabulary interface

### EventName

`EventName`

Candidate key being constrained
