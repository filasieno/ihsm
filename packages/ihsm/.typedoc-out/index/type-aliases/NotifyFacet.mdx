> **NotifyFacet**\<`N`\> = [`NotificationClient`](NotificationClient.mdx)\<`N`\>

Faceted notification surface — `actor.notify.<event>(...)` / `actor.notifyNow.<event>(...)`.

## Type Parameters

### N

`N` *extends* `object`
