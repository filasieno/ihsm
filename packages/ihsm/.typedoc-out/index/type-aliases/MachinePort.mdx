> **MachinePort**\<`T`\> = `T` *extends* `object` ? `Port` : `never`

Extracts the **port** type from a [TopState](../classes/TopState.mdx) subclass.

## Type Parameters

### T

`T`

A [TopState](../classes/TopState.mdx) subclass (instance type)
