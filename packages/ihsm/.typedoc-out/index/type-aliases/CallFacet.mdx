> **CallFacet**\<`S`\> = [`ServiceClient`](ServiceClient.mdx)\<`S`\>

Faceted call surface — `actor.call.<service>(...)` returns `Promise<Reply>`.
Delivery mode is chosen by the facet at the call site, not inferred.

## Type Parameters

### S

`S` *extends* `object`
