> **EventKeys**\<`Protocol`\> = `Protocol` *extends* `undefined` ? `string` : `Exclude`\<`{ [K in keyof Protocol]-?: IsServiceMethod<Protocol[K]> extends true ? never : K }`\[keyof `Protocol`\], keyof [`State`](../interfaces/State.mdx)\<`any`, `any`\>\>

Union of protocol keys whose handlers are **events** — postable with [Base.post](../interfaces/Base.mdx#post). Reserved
[State](../interfaces/State.mdx) method names and service-shaped keys are excluded; resolves to `string` for the
untyped (`undefined`) protocol.

## Type Parameters

### Protocol

`Protocol` *extends* \{ \} \| `undefined`
