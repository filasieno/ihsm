Terminal sink state class used when the runtime cannot recover from an error.

Assign or transition here from custom [StateEvents.onError](../interfaces/StateEvents.mdx#onerror) handlers when you need a
well-defined quiescent state. Display name is pre-registered as `'FatalErrorState'`.

## Extends

- [`TopState`](TopState.mdx)\<`Context`, `Protocol`\>

## Type Parameters

### Context

`Context`

### Protocol

`Protocol` *extends* \{ \} \| `undefined`

## Constructors

### Constructor

> **new FatalErrorState**\<`Context`, `Protocol`\>(): `FatalErrorState`\<`Context`, `Protocol`\>

#### Returns

`FatalErrorState`\<`Context`, `Protocol`\>

#### Inherited from

[`TopState`](TopState.mdx).[`constructor`](TopState.mdx#constructor)

## Properties

### ctx

> `readonly` **ctx**: `Context`

Domain context (injected by runtime — do not assign in constructors).

#### Inherited from

[`TopState`](TopState.mdx).[`ctx`](TopState.mdx#ctx)

***

### hsm

> `readonly` **hsm**: [`State`](../interfaces/State.mdx)\<`Context`, `Protocol`\>

Handler view of the machine (`this` inside methods delegates here for core operations).

#### Inherited from

[`TopState`](TopState.mdx).[`hsm`](TopState.mdx#hsm)

## Accessors

### port

#### Get Signature

> **get** **port**(): `Port`

Outbound boundary — the `port` instance passed to [makeActor](../functions/makeActor.mdx) / [testing!makeTestActor](../../testing/functions/makeTestActor.mdx);
all impure I/O flows through here.

Typed `undefined` for machines created without a port (the default), so existing
port-less machines are unaffected. At runtime a [Port](TopState.mdx#port) always backs such
machines — it is what [State.deferredPost](../interfaces/State.mdx#deferredpost) uses for its timer service.

##### Returns

`Port`

#### Inherited from

[`TopState`](TopState.mdx).[`port`](TopState.mdx#port-1)

***

### eventName

#### Get Signature

> **get** **eventName**(): `string`

Name of the event or service currently being dispatched.

Matches the string passed to [Base.post](../interfaces/Base.mdx#post), [Hsm.call](../interfaces/Hsm.mdx#call), or
[State.postNow](../interfaces/State.mdx#postnow). Empty string when no handler is running.

##### Returns

`string`

Name of the event or service currently being dispatched.

Matches the string passed to [Base.post](../interfaces/Base.mdx#post), [Hsm.call](../interfaces/Hsm.mdx#call), or
[State.postNow](../interfaces/State.mdx#postnow). Empty string when no handler is running.

#### Inherited from

[`TopState`](TopState.mdx).[`eventName`](TopState.mdx#eventname)

***

### eventPayload

#### Get Signature

> **get** **eventPayload**(): `any`[]

Arguments passed with the current dispatch, excluding injected `resolve` / `reject`
for [Hsm.call](../interfaces/Hsm.mdx#call) services.

Empty array when idle. Typed as `any[]` at runtime; correlate with
[EventPayload](../type-aliases/EventPayload.mdx) / [ServiceRequest](../type-aliases/ServiceRequest.mdx) at compile time on the client.

##### Returns

`any`[]

Arguments passed with the current dispatch, excluding injected `resolve` / `reject`
for [Hsm.call](../interfaces/Hsm.mdx#call) services.

Empty array when idle. Typed as `any[]` at runtime; correlate with
[EventPayload](../type-aliases/EventPayload.mdx) / [ServiceRequest](../type-aliases/ServiceRequest.mdx) at compile time on the client.

#### Inherited from

[`TopState`](TopState.mdx).[`eventPayload`](TopState.mdx#eventpayload)

***

### traceHeader

#### Get Signature

> **get** **traceHeader**(): `string`

Prefix for nested trace domains, built from internal dispatch stack frames.

Empty at the top level; grows like `domain|subdomain|` during nested operations.
Handlers rarely need to read this directly — it is prepended automatically by
the default [TraceWriter](../interfaces/TraceWriter.mdx).

##### Returns

`string`

Prefix for nested trace domains, built from internal dispatch stack frames.

Empty at the top level; grows like `domain|subdomain|` during nested operations.
Handlers rarely need to read this directly — it is prepended automatically by
the default [TraceWriter](../interfaces/TraceWriter.mdx).

#### Inherited from

[`TopState`](TopState.mdx).[`traceHeader`](TopState.mdx#traceheader)

***

### topState

#### Get Signature

> **get** **topState**(): [`StateClass`](../type-aliases/StateClass.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

Constructor of the root state class supplied to [makeHsm](../functions/makeHsm.mdx).

Constant for the lifetime of the instance unless you replace the entire machine.

##### Returns

[`StateClass`](../type-aliases/StateClass.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

Constructor of the root state class supplied to [makeHsm](../functions/makeHsm.mdx).

Constant for the lifetime of the instance unless you replace the entire machine.

#### Inherited from

[`TopState`](TopState.mdx).[`topState`](TopState.mdx#topstate)

***

### currentStateName

#### Get Signature

> **get** **currentStateName**(): `string`

Human-readable name of [currentState](../interfaces/Properties.mdx#currentstate).

Sourced from [defineStateName](../functions/defineStateName.mdx) / [registerStateNames](../functions/registerStateNames.mdx) when registered;
otherwise `Class.name` (unreliable under minification — register names in browser builds).

##### Returns

`string`

Human-readable name of [currentState](../interfaces/Properties.mdx#currentstate).

Sourced from [defineStateName](../functions/defineStateName.mdx) / [registerStateNames](../functions/registerStateNames.mdx) when registered;
otherwise `Class.name` (unreliable under minification — register names in browser builds).

#### Inherited from

[`TopState`](TopState.mdx).[`currentStateName`](TopState.mdx#currentstatename)

***

### currentState

#### Get Signature

> **get** **currentState**(): [`StateClass`](../type-aliases/StateClass.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

Constructor (`Function`) of the **leaf** state class currently executing.

Compare with [topState](../interfaces/Properties.mdx#topstate), which is always the root composite passed to
[makeHsm](../functions/makeHsm.mdx). After a transition, this updates to the new leaf's constructor.

##### Returns

[`StateClass`](../type-aliases/StateClass.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

Constructor (`Function`) of the **leaf** state class currently executing.

Compare with [topState](../interfaces/Properties.mdx#topstate), which is always the root composite passed to
[makeHsm](../functions/makeHsm.mdx). After a transition, this updates to the new leaf's constructor.

#### Inherited from

[`TopState`](TopState.mdx).[`currentState`](TopState.mdx#currentstate)

***

### ctxTypeName

#### Get Signature

> **get** **ctxTypeName**(): `string`

Runtime label derived from `ctx` constructor name, used as the first segment of
[traceHeader](../interfaces/Properties.mdx#traceheader) in verbose traces.

##### Returns

`string`

Runtime label derived from `ctx` constructor name, used as the first segment of
[traceHeader](../interfaces/Properties.mdx#traceheader) in verbose traces.

#### Inherited from

[`TopState`](TopState.mdx).[`ctxTypeName`](TopState.mdx#ctxtypename)

***

### traceLevel

#### Get Signature

> **get** **traceLevel**(): [`TraceLevel`](../enumerations/TraceLevel.mdx)

Active trace verbosity; changing this swaps dispatch tracing behavior immediately.

##### Returns

[`TraceLevel`](../enumerations/TraceLevel.mdx)

#### Set Signature

> **set** **traceLevel**(`value`): `void`

Active trace verbosity; changing this swaps dispatch tracing behavior immediately.

##### Parameters

###### value

[`TraceLevel`](../enumerations/TraceLevel.mdx)

##### Returns

`void`

Active trace verbosity; changing this swaps dispatch tracing behavior immediately.

#### See

TraceLevel

#### Inherited from

[`TopState`](TopState.mdx).[`traceLevel`](TopState.mdx#tracelevel)

***

### topStateName

#### Get Signature

> **get** **topStateName**(): `string`

Display name of [topState](../interfaces/Properties.mdx#topstate) (same naming rules as [currentStateName](../interfaces/Properties.mdx#currentstatename)).

##### Returns

`string`

Display name of [topState](../interfaces/Properties.mdx#topstate) (same naming rules as [currentStateName](../interfaces/Properties.mdx#currentstatename)).

#### Inherited from

[`TopState`](TopState.mdx).[`topStateName`](TopState.mdx#topstatename)

***

### traceWriter

#### Get Signature

> **get** **traceWriter**(): [`TraceWriter`](../interfaces/TraceWriter.mdx)

Destination for runtime and handler-initiated trace lines.

Replaceable at any time (e.g. swap in a test double before `post`/`call`).

##### Returns

[`TraceWriter`](../interfaces/TraceWriter.mdx)

#### Set Signature

> **set** **traceWriter**(`value`): `void`

Destination for runtime and handler-initiated trace lines.

Replaceable at any time (e.g. swap in a test double before `post`/`call`).

##### Parameters

###### value

[`TraceWriter`](../interfaces/TraceWriter.mdx)

##### Returns

`void`

Destination for runtime and handler-initiated trace lines.

Replaceable at any time (e.g. swap in a test double before `post`/`call`).

#### Inherited from

[`TopState`](TopState.mdx).[`traceWriter`](TopState.mdx#tracewriter)

***

### dispatchErrorCallback

#### Get Signature

> **get** **dispatchErrorCallback**(): [`DispatchErrorCallback`](../interfaces/DispatchErrorCallback.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

Last-resort error hook when [StateEvents.onError](../interfaces/StateEvents.mdx#onerror) / [StateEvents.onUnhandled](../interfaces/StateEvents.mdx#onunhandled)
do not recover.

##### Returns

[`DispatchErrorCallback`](../interfaces/DispatchErrorCallback.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

#### Set Signature

> **set** **dispatchErrorCallback**(`value`): `void`

Last-resort error hook when [StateEvents.onError](../interfaces/StateEvents.mdx#onerror) / [StateEvents.onUnhandled](../interfaces/StateEvents.mdx#onunhandled)
do not recover.

##### Parameters

###### value

[`DispatchErrorCallback`](../interfaces/DispatchErrorCallback.mdx)\<`Context`, [`Dispatch`](../type-aliases/Dispatch.mdx)\<`Protocol`, `InternalProtocol`\>\>

##### Returns

`void`

Last-resort error hook when [StateEvents.onError](../interfaces/StateEvents.mdx#onerror) / [StateEvents.onUnhandled](../interfaces/StateEvents.mdx#onunhandled)
do not recover.

#### See

DispatchErrorCallback

#### Inherited from

[`TopState`](TopState.mdx).[`dispatchErrorCallback`](TopState.mdx#dispatcherrorcallback)

## Methods

### transition()

> **transition**(`nextState`): `void`

Schedule an **external** transition to `nextState` after the current handler completes.

Does **not** run exit/entry immediately. When the handler returns successfully (including
after awaiting an `async` handler's Promise), the runtime:

1. Computes the **lowest common ancestor (LCA)** on the class prototype chain
2. Runs `onExit()` from the current leaf up to (but not including) the LCA
3. Switches the instance prototype to `nextState` (descending `@InitialState` chains for composites)
4. Runs `onEntry()` down from the LCA to the target leaf

#### Parameters

##### nextState

[`StateClass`](../type-aliases/StateClass.mdx)\<`Context`, `Protocol`\>

Destination state **class** constructor (not an instance)

#### Returns

`void`

#### Remarks

- **Self-transition** to the same leaf with unchanged initial descent: optimized to skip exit/entry
- **Internal transition:** omit `transition()` — active class unchanged, no exit/entry
- **`transition()` inside `onEntry`/`onExit`:** scheduled transition is **cleared** when that lifecycle dispatch ends — use [post](../interfaces/State.mdx#post) from `onEntry` for follow-up work
- Transition paths are **cached** per `From=>To` pair for hot loops
- Only the **last** `transition()` call wins if invoked multiple times in one handler

#### Inherited from

[`TopState`](TopState.mdx).[`transition`](TopState.mdx#transition)

***

### unhandled()

> **unhandled**(): `never`

Declare that the current event has **no handler** on this state (explicit super-call pattern).

Throws [UnhandledEventError](UnhandledEventError.mdx) unless an ancestor's [StateEvents.onUnhandled](../interfaces/StateEvents.mdx#onunhandled)
catches it. Prefer **omitting** the method entirely when a state should inherit a parent's
handler — only call `unhandled()` when you intentionally defer to the error model.

#### Returns

`never`

`never` — always throws or redirects via `onUnhandled`

#### Remarks

Runtime dispatch already throws when no method exists on the prototype chain; `unhandled()`
is for handlers that exist but choose not to handle the event.

#### Inherited from

[`TopState`](TopState.mdx).[`unhandled`](TopState.mdx#unhandled)

***

### sleep()

> **sleep**(`millis`): `Promise`\<`void`\>

Pause the **current handler** without blocking the JavaScript event loop.

Returns a Promise resolved after `millis` milliseconds via `setTimeout`. The actor
remains **locked** to this handler until the Promise settles (run-to-completion) — other
`post`/`call` jobs queue but do not run.

#### Parameters

##### millis

`number`

Sleep duration in milliseconds (≥ 0)

#### Returns

`Promise`\<`void`\>

Promise that resolves (never rejects) when the delay elapses

#### Remarks

Use for simple delays inside handlers. For calendar-time deferral of **new** events,
prefer [deferredPost](../interfaces/State.mdx#deferredpost). Composable with `async` handlers: `await this.sleep(100)`.

#### Inherited from

[`TopState`](TopState.mdx).[`sleep`](TopState.mdx#sleep)

***

### post()

> **post**\<`EventName`\>(`eventName`, ...`eventPayload`): `void`

Enqueue a **normal-priority** event for later dispatch on the active state.

Returns immediately; the handler runs asynchronously when the actor reaches this job.
Dispatch walks the **prototype chain** from the current leaf upward until a method named
`eventName` is found.

#### Type Parameters

##### EventName

`EventName` *extends* `string` \| `number` \| `symbol`

Literal key of `Protocol` being posted

#### Parameters

##### eventName

[`PostedEvent`](../type-aliases/PostedEvent.mdx)\<`Protocol`, `EventName`\>

Event or service name. Must be `keyof Protocol` and must **not** collide
  with reserved [State](../interfaces/State.mdx) method names (`transition`, `post`, `ctx`, …)

##### eventPayload

...[`EventPayload`](../type-aliases/EventPayload.mdx)\<`Protocol`, `EventName`\>

Arguments tuple inferred from `Protocol[eventName]` handler parameters.
  For **events**, pass every parameter **except** `resolve` / `reject`. For fire-and-forget
  events, the handler return type must be `void` or `Promise<void>`

#### Returns

`void`

#### Remarks

**Client usage:** `door.post('open')` then `await door.sync()` to wait for completion.

**Handler usage:** `this.post('tick')` schedules work **after** the current handler returns
and after any [State.transition](../interfaces/State.mdx#transition) it requested. Normal-priority posts run **after** all
[State.postNow](../interfaces/State.mdx#postnow) hi-priority jobs drained for the current turn.

**Ordering:** FIFO among normal-priority jobs. Multiple posts before one `sync()` are
processed in submission order.

**Typing:** With `Protocol extends undefined`, accepts any `string` and `any[]` (legacy mode).

#### Inherited from

[`TopState`](TopState.mdx).[`post`](TopState.mdx#post)

***

### deferredPost()

> **deferredPost**\<`EventName`\>(`millis`, `eventName`, ...`eventPayload`): `void`

Schedule a normal-priority [post](../interfaces/State.mdx#post) after a wall-clock delay. **Handler-only.**

Backed by the machine's [Port](Port.mdx) timer service: when no custom port is supplied the
runtime instantiates a [Port](Port.mdx) whose `setTimeout`-based timer is used here. When
the timer fires, the event is enqueued like an ordinary `post`. This is intentionally **not**
on the external [Hsm](../interfaces/Hsm.mdx) / [Actor](../type-aliases/Actor.mdx) surface — clients schedule work via [post](../interfaces/State.mdx#post).

#### Type Parameters

##### EventName

`EventName` *extends* `string` \| `number` \| `symbol`

Literal key of `Protocol` being scheduled

#### Parameters

##### millis

`number`

Delay in milliseconds before enqueueing (≥ 0). Subject to timer granularity

##### eventName

[`PostedEvent`](../type-aliases/PostedEvent.mdx)\<`Protocol`, `EventName`\>

Event name (same constraints as [post](../interfaces/State.mdx#post))

##### eventPayload

...[`EventPayload`](../type-aliases/EventPayload.mdx)\<`Protocol`, `EventName`\>

Handler arguments tuple (same as [post](../interfaces/State.mdx#post))

#### Returns

`void`

#### Remarks

Does **not** block the calling handler — returns as soon as the timer is registered. Timers
are **not** cancelled if the machine transitions or the scheduling handler throws. (Richer
timer services — cancellation, virtual clocks — will arrive later through the port.)

#### Inherited from

[`TopState`](TopState.mdx).[`deferredPost`](TopState.mdx#deferredpost)

***

### postNow()

> **postNow**\<`EventName`\>(`eventName`, ...`eventPayload`): `void`

Enqueue a **hi-priority** event processed before normal [post](../interfaces/State.mdx#post) jobs from the same turn.

Only valid **inside** a running handler (`this.postNow`). Clients must use [post](../interfaces/State.mdx#post).
Hi-priority jobs drain immediately after the current handler and its transition complete,
**before** any normal-priority posts the handler enqueued (including chained `this.post`).

#### Type Parameters

##### EventName

`EventName` *extends* `string` \| `number` \| `symbol`

Literal key of `Protocol`

#### Parameters

##### eventName

[`PostedEvent`](../type-aliases/PostedEvent.mdx)\<`Protocol`, `EventName`\>

Event name (same typing rules as [post](../interfaces/State.mdx#post))

##### eventPayload

...[`EventPayload`](../type-aliases/EventPayload.mdx)\<`Protocol`, `EventName`\>

Handler arguments (same tuple as [post](../interfaces/State.mdx#post))

#### Returns

`void`

#### Remarks

Models **extended transitions**: multiple internal steps (lock, capture, validate) that
must complete before deferred side effects. See tutorial `17-post-now`.

Multiple `postNow` calls run in FIFO order within the hi-priority queue. You may need
an extra [Hsm.sync](../interfaces/Hsm.mdx#sync) after the first to drain postNow follow-ups.

#### Inherited from

[`TopState`](TopState.mdx).[`postNow`](TopState.mdx#postnow)

***

### onExit()

> **onExit**(): `void` \| `Promise`\<`void`\>

Invoked when **leaving** this state during an external transition.

Runs during the exit phase **after** the triggering handler completes and before the
prototype switches away. Async `onExit` is awaited before continuing up the LCA path.

#### Returns

`void` \| `Promise`\<`void`\>

#### Remarks

Not called for internal transitions (handler returns without [State.transition](../interfaces/State.mdx#transition)).
Not called on [Hsm.restore](../interfaces/Hsm.mdx#restore). Self-transitions may skip exit when optimized.

#### Inherited from

[`TopState`](TopState.mdx).[`onExit`](TopState.mdx#onexit)

***

### onEntry()

> **onEntry**(): `void` \| `Promise`\<`void`\>

Invoked when **entering** this state during initialization or an external transition.

Runs during the entry phase after the prototype points at this state. Async `onEntry` is
awaited before entering nested initial substates or running deeper `onEntry` hooks.

#### Returns

`void` \| `Promise`\<`void`\>

#### Remarks

During `makeHsm(..., initialize: true)`, `onEntry` runs from root down through each
`@InitialState` chain to the initial leaf. Schedule follow-up transitions via [Base.post](../interfaces/Base.mdx#post),
not [State.transition](../interfaces/State.mdx#transition), from within `onEntry`.

#### Inherited from

[`TopState`](TopState.mdx).[`onEntry`](TopState.mdx#onentry)

***

### onError()

> **onError**\<`EventName`\>(`error`): `void` \| `Promise`\<`void`\>

Recovery hook when an event handler throws [EventHandlerError](EventHandlerError.mdx).

#### Type Parameters

##### EventName

`EventName` *extends* `string` \| `number` \| `symbol`

Correlated event key from `Protocol`

#### Parameters

##### error

[`RuntimeError`](RuntimeError.mdx)\<`Context`, `Protocol`, `EventName`\>

Typed runtime error with [RuntimeError.eventName](RuntimeError.mdx#eventname-1),
  [RuntimeError.eventPayload](RuntimeError.mdx#eventpayload), [HsmError.context](HsmError.mdx#context-1), and [HsmError.cause](HsmError.mdx#cause)

#### Returns

`void` \| `Promise`\<`void`\>

#### Remarks

Default [TopState](TopState.mdx) implementation rethrows. Override to log, transition to a safe
state, or swallow. Uncaught errors invoke [Properties.dispatchErrorCallback](../interfaces/Properties.mdx#dispatcherrorcallback).

#### Inherited from

[`TopState`](TopState.mdx).[`onError`](TopState.mdx#onerror)

***

### onUnhandled()

> **onUnhandled**\<`EventName`\>(`error`): `void` \| `Promise`\<`void`\>

Recovery hook when dispatch would raise [UnhandledEventError](UnhandledEventError.mdx).

#### Type Parameters

##### EventName

`EventName` *extends* `string` \| `number` \| `symbol`

Correlated event key from `Protocol`

#### Parameters

##### error

[`UnhandledEventError`](UnhandledEventError.mdx)\<`Context`, `Protocol`, `EventName`\>

Describes the unmatched event and active state

#### Returns

`void` \| `Promise`\<`void`\>

#### Remarks

Override to implement catch-all handlers, auditing, or transition to an error state
without defining every event on every leaf.

#### Inherited from

[`TopState`](TopState.mdx).[`onUnhandled`](TopState.mdx#onunhandled)
