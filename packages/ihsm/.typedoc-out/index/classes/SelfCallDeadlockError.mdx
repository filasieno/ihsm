Thrown in debug builds when a service targets the machine currently dispatching.

## Extends

- `Error`

## Constructors

### Constructor

> **new SelfCallDeadlockError**(): `SelfCallDeadlockError`

#### Returns

`SelfCallDeadlockError`

#### Overrides

`Error.constructor`
