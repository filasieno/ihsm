Thrown when an abstract `@`[mock](../functions/mock.mdx) port method is called but the test scripted no
implementation for it. The message names the method so the fix is obvious.

## Extends

- `Error`

## Constructors

### Constructor

> **new PreloadError**(`method`): `PreloadError`

#### Parameters

##### method

`string`

#### Returns

`PreloadError`

#### Overrides

`Error.constructor`
