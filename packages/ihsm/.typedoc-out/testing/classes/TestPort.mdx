Runtime port binding — `actor` is set synchronously before factory return.

## Extends

- [`BasePort`](../../index/classes/BasePort.mdx)\<`T`\>

## Type Parameters

### T

`T` = [`Any`](../../index/type-aliases/Any.mdx)

## Implements

- [`RandomService`](../../index/interfaces/RandomService.mdx)

## Constructors

### Constructor

> **new TestPort**\<`T`\>(): `TestPort`\<`T`\>

#### Returns

`TestPort`\<`T`\>

#### Inherited from

[`BasePort`](../../index/classes/BasePort.mdx).[`constructor`](../../index/classes/BasePort.mdx#constructor)

## Properties

### \_\_topState

> `readonly` **\_\_topState**: `T`

#### Inherited from

[`BasePort`](../../index/classes/BasePort.mdx).[`__topState`](../../index/classes/BasePort.mdx#__topstate)

***

### actor

> **actor**: [`InboundActor`](../../index/type-aliases/InboundActor.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\> \| [`ChildActor`](../../index/type-aliases/ChildActor.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\>

#### Inherited from

[`BasePort`](../../index/classes/BasePort.mdx).[`actor`](../../index/classes/BasePort.mdx#actor)

## Accessors

### messages

#### Get Signature

> **get** **messages**(): readonly [`TracedMessage`](../../index/interfaces/TracedMessage.mdx)[]

Every recorded message, in order.

##### Returns

readonly [`TracedMessage`](../../index/interfaces/TracedMessage.mdx)[]

***

### events

#### Get Signature

> **get** **events**(): readonly `string`[]

Just the recorded event names, in order.

##### Returns

readonly `string`[]

***

### trace

#### Get Signature

> **get** **trace**(): readonly `string`[]

Rendered `event` / `event:arg,arg` strings — convenient for `deep.equal` / `include`.

##### Returns

readonly `string`[]

***

### last

#### Get Signature

> **get** **last**(): [`TracedMessage`](../../index/interfaces/TracedMessage.mdx) \| `undefined`

The most recently recorded message, or `undefined`.

##### Returns

[`TracedMessage`](../../index/interfaces/TracedMessage.mdx) \| `undefined`

***

### count

#### Get Signature

> **get** **count**(): `number`

Number of recorded messages.

##### Returns

`number`

***

### now

#### Get Signature

> **get** **now**(): `number`

Current virtual time, in milliseconds since construction.

##### Returns

`number`

***

### pending

#### Get Signature

> **get** **pending**(): `number`

Number of timers still pending (not yet fired or disposed).

##### Returns

`number`

## Methods

### record()

> **record**(`event`, ...`payload`): `void`

Append an entry to the recorded message log (for assertions / golden traces).

Callable both from inside the mock (e.g. a port method logging its outbound call) and from a
test (e.g. a `Disposable.dispose` closure recording its own teardown into the trace).

#### Parameters

##### event

`string`

Event name or free-form label (e.g. `'connect'`, `'attempt 1: ok'`)

##### payload

...`unknown`[]

Optional values associated with the entry

#### Returns

`void`

***

### send()

> **send**(`event`, ...`payload`): `void`

Push an **inbound** internal notification into the bound actor (`port.actor.onData(…)`).

Convenience for deterministic tests — equivalent to calling the generated method on
[BasePort.actor](../../index/classes/BasePort.mdx#actor) after the factory has wired it.

#### Parameters

##### event

`string`

##### payload

...`unknown`[]

#### Returns

`void`

***

### clear()

> **clear**(): `void`

Clear the recorded message log.

#### Returns

`void`

***

### setTimeout()

> **setTimeout**(`callback`, `millis?`): `number`

#### Parameters

##### callback

() => `void`

##### millis?

`number`

#### Returns

`number`

#### Inherit Doc

— backed by the virtual clock; fire with [advance](#advance).

***

### clearTimeout()

> **clearTimeout**(`id`): `void`

#### Parameters

##### id

`number` \| `undefined`

#### Returns

`void`

#### Inherit Doc

***

### setInterval()

> **setInterval**(`callback`, `millis?`): `number`

#### Parameters

##### callback

() => `void`

##### millis?

`number`

#### Returns

`number`

#### Inherit Doc

— backed by the virtual clock; fire with [advance](#advance).

***

### clearInterval()

> **clearInterval**(`id`): `void`

#### Parameters

##### id

`number` \| `undefined`

#### Returns

`void`

#### Inherit Doc

***

### advance()

> **advance**(`millis`): `void`

Advance the virtual clock by `millis`, firing every timer whose deadline is reached, in
deadline order. Timers scheduled by a fired callback within the same window are **not** run
until a later `advance` — mirroring how a deferred re-schedule lands on the next run-to-completion turn.

#### Parameters

##### millis

`number`

Virtual milliseconds to advance (negative values are clamped to `0`)

#### Returns

`void`

***

### feedRandom()

> **feedRandom**(...`values`): `void`

Queue values returned by successive [random](#random) calls (FIFO).

#### Parameters

##### values

...`number`[]

#### Returns

`void`

***

### feedCryptoRandom()

> **feedCryptoRandom**(...`values`): `void`

Queue values returned by successive [cryptoRandom](#cryptorandom) calls (FIFO).

#### Parameters

##### values

...`number`[]

#### Returns

`void`

***

### feedUUID()

> **feedUUID**(...`values`): `void`

Queue values returned by successive [randomUUID](#randomuuid) calls (FIFO).

#### Parameters

##### values

...`string`[]

#### Returns

`void`

***

### feedRandomBytes()

> **feedRandomBytes**(...`bytes`): `void`

Queue bytes used to fill arrays in successive [getRandomValues](#getrandomvalues) calls (FIFO).

#### Parameters

##### bytes

...`number`[]

#### Returns

`void`

***

### resetRandom()

> **resetRandom**(): `void`

Clear all scripted random queues ([feedRandom](#feedrandom) / [feedCryptoRandom](#feedcryptorandom) / …).

#### Returns

`void`

***

### random()

> **random**(): `number`

#### Returns

`number`

#### Inherit Doc

— queued values only; returns `0` when empty (never calls `Math.random`).

#### Implementation of

[`RandomService`](../../index/interfaces/RandomService.mdx).[`random`](../../index/interfaces/RandomService.mdx#random)

***

### cryptoRandom()

> **cryptoRandom**(): `number`

#### Returns

`number`

#### Inherit Doc

— queued values only; returns `0` when empty (never calls `crypto.random`).

#### Implementation of

[`RandomService`](../../index/interfaces/RandomService.mdx).[`cryptoRandom`](../../index/interfaces/RandomService.mdx#cryptorandom)

***

### randomUUID()

> **randomUUID**(): `string`

#### Returns

`string`

#### Inherit Doc

— queued values only; never calls `crypto.randomUUID`.

#### Implementation of

[`RandomService`](../../index/interfaces/RandomService.mdx).[`randomUUID`](../../index/interfaces/RandomService.mdx#randomuuid)

***

### getRandomValues()

> **getRandomValues**\<`T`\>(`array`): `T`

#### Type Parameters

##### T

`T` *extends* `ArrayBufferView`\<`ArrayBufferLike`\>

#### Parameters

##### array

`T`

#### Returns

`T`

#### Inherit Doc

— queued bytes only; never calls `crypto.getRandomValues`.

#### Implementation of

[`RandomService`](../../index/interfaces/RandomService.mdx).[`getRandomValues`](../../index/interfaces/RandomService.mdx#getrandomvalues)
