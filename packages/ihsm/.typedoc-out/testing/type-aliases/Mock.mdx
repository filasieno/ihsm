> **Mock**\<`P`, `C`\> = `P` & `{ [K in keyof ActorPortOf<C>]: ActorPortOf<C>[K] extends (args: infer A) => infer R ? Stubbed<A, R> : ActorPortOf<C>[K] }`

The fully-wired mock type returned by [makeTestPort](../functions/makeTestPort.mdx): the mock class `P` itself, with each
**port method** (inferred from the machine's [TopState](../../index/classes/TopState.mdx) via ConfigPort) upgraded to
a scriptable, introspectable [Stubbed](../interfaces/Stubbed.mdx) method.

## Type Parameters

### P

`P`

The mock port class instance type

### C

`C` *extends* [`ActorConfig`](../../index/interfaces/ActorConfig.mdx) = [`ActorConfig`](../../index/interfaces/ActorConfig.mdx)

The machine [ActorConfig](../../index/interfaces/ActorConfig.mdx) bag (inferred from the port's [TestPort](../classes/TestPort.mdx) type param)
