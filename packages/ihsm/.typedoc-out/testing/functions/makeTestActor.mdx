> **makeTestActor**\<`T`\>(`topState`, `ctx`, `port?`, `options?`): [`TestActor`](../type-aliases/TestActor.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\>

Creates a **full-access** actor for deterministic tests: merged protocol + typed `port`.

Identical construction to [makeActor](../../index/functions/makeActor.mdx) (same three mandatory arguments + [ActorOptions](../../index/interfaces/ActorOptions.mdx)),
but the returned [TestActor](../type-aliases/TestActor.mdx) exposes the merged Dispatch protocol — so tests can
drive internal events directly (no live port required) — and grants typed access to `port` for
asserting outbound interactions or pushing observations.

Unlike [makeActor](../../index/functions/makeActor.mdx), `traceLevel` defaults to [TraceLevel.VERBOSE\_DEBUG](../../index/enumerations/TraceLevel.mdx#verbose_debug) so a failing
test is fully readable; opt down explicitly via `options.traceLevel` only when you need a quiet run.

## Type Parameters

### T

`T` *extends* [`TopStateArg`](../../index/type-aliases/TopStateArg.mdx)\<[`ActorConfig`](../../index/interfaces/ActorConfig.mdx)\>

## Parameters

### topState

`ValidatedTopStateArg`\<`T`\>

Root state class; `Context` / `Public` / `Internal` are inferred from it (see [TopStateArg](../../index/type-aliases/TopStateArg.mdx))

### ctx

[`ActorContextOf`](../../index/type-aliases/ActorContextOf.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\>

Mutable domain object shared by all states

### port?

[`TestPort`](../classes/TestPort.mdx)\<[`Any`](../../index/type-aliases/Any.mdx)\> \| [`MachinePortInput`](../../index/type-aliases/MachinePortInput.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\>

Outbound [Port](../../index/classes/Port.mdx) instance (its `actor` is bound by the factory)

### options?

[`ActorOptions`](../../index/interfaces/ActorOptions.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\> = `{}`

Optional tuning: `initialize` / `traceLevel` / `traceWriter` / … (see [ActorOptions](../../index/interfaces/ActorOptions.mdx))

## Returns

[`TestActor`](../type-aliases/TestActor.mdx)\<[`ActorConfigOf`](../../index/type-aliases/ActorConfigOf.mdx)\<`T`\>\>

A [TestActor](../type-aliases/TestActor.mdx) handle with full event + port access

## Example

```ts
const sm = makeTestActor(ConnTop, new ConnCtx(), ihsmTest.makeTestPort(ConnMock));
```
